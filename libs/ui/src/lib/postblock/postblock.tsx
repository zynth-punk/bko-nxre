import React from 'react';

import './postblock.scss';

/* eslint-disable-next-line */
export interface PostblockProps {}

export const Postblock = (props: PostblockProps) => {
  return (
    <div>
      <h1>Welcome to postblock!</h1>
    </div>
  );
};

export default Postblock;
